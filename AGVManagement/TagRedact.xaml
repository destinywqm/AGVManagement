<Window x:Class="AGVManagement.TagRedact"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="信标编辑" Height="200" Width="300" ResizeMode="CanMinimize" WindowStartupLocation="CenterScreen" Icon="Images/笔.png">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <!-- 修改此行定义，以自动分配剩余空间 -->
        </Grid.RowDefinitions>
        <Label Content="X轴：" Grid.Row="0" Grid.Column="0" HorizontalAlignment="Center" VerticalContentAlignment="Center"/>
        <TextBox Name="TagX" Grid.Row="0" Grid.Column="1" TextChanged="TagX_TextChanged"/>
        <Label Content="Y轴：" Grid.Row="1" Grid.Column="0" HorizontalAlignment="Center" VerticalContentAlignment="Center"/>
        <TextBox Grid.Row="1" Name="TagY" Grid.Column="1" Margin="0,5,0,10"/>

        <Button Content="确认" Name="Tag_Submit" Width="80" Margin="25,18,0,0" Grid.Row="3" VerticalAlignment="Top" HorizontalAlignment="Left" Click="Tag_Submit_Click"/>
        <Button Name="TagDelete" Content="删除" Width="80" Margin="0,18,18,0" Grid.Row="3" Grid.Column="1" VerticalAlignment="Top" HorizontalAlignment="Right" Click="TagDelete_Click"/>
    </Grid>
</Window>
